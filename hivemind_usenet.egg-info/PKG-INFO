Metadata-Version: 2.4
Name: hivemind-usenet
Version: 0.1.1a1
Summary: HiveMind transport over Usenet: carrier framing, wormhole (NetworkProtocol), and NL bridge
License: Apache-2.0
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: usenet @ git+https://github.com/TigreGotico/usenet
Requires-Dist: remailers @ git+https://github.com/TigreGotico/remailers
Requires-Dist: hivemind-plugin-manager
Requires-Dist: hivemind_bus_client
Requires-Dist: hivemind-core
Requires-Dist: ovos-utils
Requires-Dist: ovos-bus-client>=2.0.0a3

# hivemind-usenet

An **experimental** [HiveMind](https://github.com/JarbasHiveMind/HiveMind-core)
transport over **Usenet**: anonymous, store-and-forward, censorship-resistant mesh
links carried by `alt.anonymous.messages`. Nodes post PGP-encrypted, hSub-addressed
articles to a newsgroup and poll for replies — no direct connection, no shared IP,
no registered account on the public anon-post servers.

Usenet is high-latency and poll-based, so this is a covert / control-plane link or
a fallback for nodes that are never reachable directly — not a low-latency audio
channel.

## Where it sits

Standard HiveMind links are live encrypted WebSocket connections to a
[hivemind-core](https://github.com/JarbasHiveMind/HiveMind-core) hub. hivemind-usenet
swaps the carrier for Usenet while keeping the HiveMind handshake and AES session
on top, unchanged. It ships three surfaces in one package:

- **`UsenetCarrier`** — shared framing layer. Chunks arbitrary payloads into ~8 KB
  base64 frames, PGP-encrypts each to the peer's key, posts under an hSub subject,
  and reassembles on poll. Fully unit-testable without a live NNTP connection.
- **`UsenetWormhole`** (a `NetworkProtocol`) — full HiveMessage transport. A
  point-to-point link whose poll loop reassembles `kind="hive"` frames and routes
  them via `hm_protocol.handle_message`. Registered under the
  `hivemind.network.protocol` entry point.
- **`UsenetBridge`** (a `threading.Thread`) — natural-language gateway. Polls a
  code-word hSub for text posts, injects them into the hive as utterances, and
  posts the spoken answers back. Per-peer session ids and per-session FIFO queues.

A satellite-side client (`UsenetClient`) mirrors the HTTP/WS client API so the same
connect/emit/run/close usage works over the Usenet carrier.

## How it works

```
local node  ◀──── UsenetCarrier (PGP + hSub frames) ────▶  peer node
                 post to alt.anonymous.messages
                 poll + match hSub + decrypt + reassemble
```

- **Addressing** is hSub (hashed subject): a shared passphrase per peer. The sender
  stamps the subject with `create_hsub(peer_secret)`; the receiver filters articles
  with `match_hsub(subject, my_secret)`.
- **Confidentiality** is PGP: each frame is encrypted to the peer's public key.
- **Reassembly** dedupes on `(message-id, chunk)` and rebuilds the payload once all
  chunks arrive.

## Prerequisites

- Python 3.10+
- A PGP identity per node (auto-generated by `remailers.Credentials` at the
  configured `key_path` if missing).
- For each peer link, an out-of-band exchange of: an **hSub passphrase** and the
  peer's **PGP public key** (see [first contact](#first-contact--hsub-addressing)).
- Network access to a Usenet server that accepts anonymous posts. Defaults to
  `paganini.bofh.team`; `news.tcpreset.net` also accepts anon posts.

## Install

```bash
pip install hivemind-usenet
```

From source:

```bash
git clone https://github.com/JarbasHiveMind/hivemind-usenet
cd hivemind-usenet
pip install -e .
```

This pulls the `usenet` and `remailers` carrier libraries plus the HiveMind
packages.

## Quickstart

Two nodes that have exchanged passphrases + pubkeys out of band:

```bash
# Wormhole node (full HiveMessage transport)
hivemind-usenet-wormhole \
    --my-secret   "my-passphrase" \
    --peer-secret "their-passphrase" \
    --peer-pubkey /path/to/peer.asc \
    --server-url  paganini.bofh.team

# NL bridge (text in/out of a local hive)
hivemind-usenet-bridge \
    --my-secret "bridge-codeword" \
    --hive-host 127.0.0.1 \
    --hive-key  "my-hivemind-api-key"
```

`--my-secret` is the hSub passphrase you **read** with; `--peer-secret` is the one
you **post** with (the peer reads it). They are the mirror image on the other node.

## First contact / hSub addressing

hSub addressing is **shared-secret symmetric**: both peers must agree on a
passphrase and exchange PGP public keys out of band before any post. There is no
in-band key exchange in v1; a discovery "lobby" hSub is a planned stretch goal.

## Configuration

CLI flags map to config keys read by each component. See
[configuration](docs/configuration.md) for the full table and the JSON config-file
form.

## Documentation

See [`docs/`](docs/index.md):

- [How it works](docs/how-it-works.md) — carrier framing, hSub, PGP, reassembly.
- [Components](docs/components.md) — carrier, wormhole, bridge, client.
- [Configuration](docs/configuration.md) — every CLI flag and config key.
- [Examples](docs/examples.md) — the live-network demos under `examples/`.

## License

Apache-2.0
